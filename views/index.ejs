<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ToDo App</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <div class="container">
    <h1>ToDo Список</h1>
  
    <% if (typeof errors !== 'undefined' && errors.length > 0) { %>
      <div class="error-messages">
        <% errors.forEach(error => { %>
          <p class="error"><%= error.msg %></p>
        <% }); %>
      </div>
    <% } %>
    
    <form action="/tasks" method="POST" class="add-form">
      <div class="form-group">
        <label for="title">Название задачи *:</label>
        <input type="text" id="title" name="title" value="<%= typeof inputData !== 'undefined' ? inputData.title : '' %>" required minlength="3" placeholder="Введите название...">
      </div>
      
      <div class="form-group">
        <label for="description">Описание (опционально):</label>
        <input type="text" id="description" name="description" value="<%= typeof inputData !== 'undefined' ? inputData.description : '' %>" maxlength="500" placeholder="Краткое описание...">
      </div>
      
      <button type="submit" class="btn-submit">➕ Добавить Задачу</button>
    </form>
    
    <ul class="task-list">
      <% if (todos.length === 0) { %>
        <li class="no-tasks">Нет задач. Добавьте первую! 🎉</li>
      <% } else { %>
        <% todos.forEach(todo => { %>
          <li class="task-item <%= todo.completed ? 'completed' : '' %>">
            <form action="/tasks/<%= todo._id %>/toggle" method="POST" class="checkbox-form">
              <input type="checkbox" <%= todo.completed ? 'checked' : '' %> onchange="this.form.submit()">
            </form>
            <div class="task-text">
              <strong><%= todo.title %></strong><br>
              <small><%= todo.description %></small>
            </div>
            <div class="task-actions">
              <a href="/tasks/<%= todo._id %>/edit" class="edit-btn">✏️ Редактировать</a>
              <form action="/tasks/<%= todo._id %>/delete" method="POST" style="display:inline;" onsubmit="return confirm('Вы уверены, что хотите удалить эту задачу?')">
                <button type="submit" class="delete-btn">🗑️ Удалить</button>
              </form>
            </div>
          </li>
        <% }); %>
      <% } %>
    </ul>
  </div>
</body>
</html>
