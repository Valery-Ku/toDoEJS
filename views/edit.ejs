<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Редактировать задачу</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <div class="container">
    <h1>Редактировать Задачу ✏️</h1>
    <a href="/" class="btn-back">← Назад к списку</a>

    <form action="/tasks/<%= todo._id %>/update" method="POST" class="edit-form">
      <div class="form-group">
        <label for="title">Название задачи *:</label>
        <input type="text" id="title" name="title" value="<%= todo.title %>" required minlength="3">
      </div>

      <div class="form-group">
        <label for="description">Описание:</label>
        <textarea id="description" name="description" rows="4"><%= todo.description %></textarea>
      </div>

      <button type="submit" class="btn-submit">Сохранить Изменения</button>
    </form>
  </div>
</body>
</html>
