<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Редактировать задачу</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <div class="container">
    <h1>Редактировать Задачу ✏️</h1>
    <a href="/" class="btn-back">← Назад к списку</a>
  
    <% if (typeof errors !== 'undefined' && errors.length > 0) { %>
      <div class="error-messages">
        <% errors.forEach(error => { %>
          <p class="error"><%= error.msg %></p>
        <% }); %>
      </div>
    <% } %>
    
    <form action="/tasks/<%= todo._id %>/update" method="POST" class="edit-form">
      <div class="form-group">
        <label for="title">Название задачи *:</label>
        <input type="text" id="title" name="title" value="<%= typeof inputData !== 'undefined' && inputData.title ? inputData.title : todo.title %>" required minlength="3">
      </div>

      <div class="form-group">
        <label for="description">Описание:</label>
        <textarea id="description" name="description" rows="4"><%= typeof inputData !== 'undefined' && inputData.description ? inputData.description : todo.description %></textarea>
      </div>

      <button type="submit" class="btn-submit">Сохранить Изменения</button>
    </form>
  </div>
</body>
</html>